# STRM 自动修复与同步加速概览

## 完成内容

- 已索引且目标路径一致的 `.strm` 被误改后，完整同步或可信增量同步会自动恢复正确播放地址。
- 没有数据库索引归属的未知同名 `.strm` 仍拒绝覆盖。
- 完整同步增加有界的本地 STRM 只读校验并发：默认 8 路，最大 32 路，可通过 `STRM_VERIFY_WORKERS` 调整。
- 只有索引中的路径、ETag、大小均与远端候选一致时才读取本地 STRM；索引已确定变化的候选直接进入更新流程，避免无意义磁盘预读。
- STRM 内容校验由 `is_file + read_bytes` 两次文件系统访问收敛为一次 `read_bytes`。
- 每批所有只读校验完成后才开始文件替换；实际 STRM 安装和 SQLite 索引写入仍保持稳定顺序，保留所有权、回滚和冲突保护。
- 指纹批量补写提交前会与最终内存索引再次核对，避免旧路径快照覆盖本轮的新索引。

## 新增观测指标

- `verify_workers_configured`：本地 STRM 校验并发配置。
- `verified_candidates`：实际读取并校验的稳定候选数量。
- `verify_prefiltered`：因索引已确定变化而跳过预读的候选数量。

## 安全边界

- 文件改名或移动时，旧路径删除仍要求原内容指纹一致。
- NFO、字幕、海报等伴随元数据仍保留严格指纹保护。
- 失效清理和来源退役清理仍不会删除被本地修改的文件。
- 未并发执行 STRM 写文件或 SQLite 更新，避免削弱逐文件事务回滚语义。

## 验证结果

- 第一轮性能与安全回归：77 项通过，另有 14 个子测试通过。
- 核心自动修复、性能与元数据复查：32 项通过。
- 增量同步、扫描并发、变化队列和调度模式复查：54 项通过。
- Python 语法编译检查通过。
- `git diff --check` 通过。
- 一次大范围合跑中出现 1 次既有元数据并发测试偶发失败；单独复现以及与核心测试复跑均通过，该路径未被本次视频校验优化修改。

## 修改文件

- `app/modules/strm.py`
- `app/modules/scheduler.py`
- `tests/test_organize_p0_performance.py`
- `tests/test_p2_pipeline_hardening.py`

## 后续可选优化

- 结合真实任务的 `scan_elapsed_seconds`、`generate_elapsed_seconds`、`request_p95_ms` 和新增校验指标，再决定是否提高云端扫描并发。
- SQLite 新增/更新索引批量提交暂未实施，原因是需要重新设计文件与数据库的批量回滚协议。
- 调度层可进一步改为高频增量同步、低频完整校准，但这属于行为策略变更，建议单独实施和验证。

## 工作树说明

工作树中另有光鸭目录刮削页面、CSS 和 RSS UI 测试等既存修改，本次没有回退或覆盖它们。